package com.example.fakecallapp

import androidx.appcompat.app.AppCompatDelegate
import androidx.compose.runtime.mutableStateOf
import com.example.fakecallapp.service_var.service_var



enum class IsDarkmode (val isDarkmode: Boolean = false){

    Dark(true),
    Light,
    System
}

fun main() {
//    for (color in RGB.values()) println(color.toString()) // prints RED, GREEN, BLUE
//    println("The first color is: ${RGB.valueOf("RED")}") // prints "The first color is: RED"
}